{
  "name": "movie-scraper-api",
  "version": "1.0.0",
  "description": "API for scraping movies, seasons, episodes, and extracting m3u8 links",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "express": "^4.19.2",
    "puppeteer": "^22.4.0"
  }
}
